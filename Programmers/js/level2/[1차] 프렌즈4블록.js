function solution(m, n, board) {
  var answer = 0;

  let prevRow = {};
  let nextRow = {};

  board.forEach((row) => {
    for(let i = 0; i < row.length; i++){
      
    }
  })

  return answer;
}

const theAnswer = solution(4,	5,	["CCBDE", "AAADE", "AAABF", "CCBBF"])